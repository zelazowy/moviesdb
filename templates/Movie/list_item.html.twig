<div class="row">
    <div class="col s12 m12">
        <div class="card blue-grey lighten-5">
            <div class="card-content deep-purple-text darken-4">
                <span class="card-title">
                    {{ movie.title }}
                </span>
                <p>
                    {{ movie.tagline }}
                </p>
            </div>
            <div class="card-action">
                <a href="{{ url('movie_details', { 'id': movie.id, 'page': page }) }}">Show</a>
            </div>
        </div>
    </div>
</div>
